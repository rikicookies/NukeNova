{% extends "layouts/default.twig" %}
{% block title %}{{ trans('welcome::title') }} — NovaNuke{% endblock %}
{% block content %}
<section class="welcome-card">
    <p class="eyebrow">{{ trans('welcome::eyebrow') }}</p>
    <h1>{{ trans('welcome::title') }}</h1>
    <p>{{ message }}</p>
    <p><a class="auth-link" href="/">{{ trans('welcome::return_home') }}</a></p>
</section>
{% endblock %}
